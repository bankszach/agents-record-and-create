"""Backend package for the timesheet agent example.

This package contains orchestration, schema/validation, parsing, IO, and export
interfaces used by the example. Implementations are intentionally minimal at
this stage and will be expanded iteratively.
"""
